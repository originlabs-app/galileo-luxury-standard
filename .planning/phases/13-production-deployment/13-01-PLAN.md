---
phase: 13-production-deployment
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/src/app/layout.tsx
  - website/src/app/opengraph-image.tsx
  - website/src/app/robots.ts
  - website/src/app/sitemap.ts
  - website/next.config.ts
autonomous: true

must_haves:
  truths:
    - "Site has proper meta title, description, and canonical URL"
    - "OG image renders with obsidian design (logo + tagline)"
    - "Twitter cards show summary_large_image"
    - "robots.txt allows all crawlers"
    - "sitemap.xml lists all public pages"
  artifacts:
    - path: "website/src/app/layout.tsx"
      provides: "Root metadata with metadataBase"
      contains: "metadataBase"
    - path: "website/src/app/opengraph-image.tsx"
      provides: "Static OG image generation"
      exports: ["default", "alt", "size", "contentType"]
    - path: "website/src/app/robots.ts"
      provides: "Crawling rules"
      exports: ["default"]
    - path: "website/src/app/sitemap.ts"
      provides: "Dynamic sitemap"
      exports: ["default"]
    - path: "website/next.config.ts"
      provides: "Build-time TypeScript/ESLint enforcement"
      contains: "ignoreBuildErrors: false"
  key_links:
    - from: "website/src/app/layout.tsx"
      to: "metadataBase URL"
      via: "metadata.metadataBase"
      pattern: "galileoprotocol\\.io"
    - from: "website/src/app/opengraph-image.tsx"
      to: "ImageResponse API"
      via: "next/og import"
      pattern: "ImageResponse"
---

<objective>
Set up comprehensive SEO and metadata for production deployment on galileoprotocol.io.

Purpose: Ensure proper social sharing (OG/Twitter cards), search engine discoverability, and canonical URLs pointing to production domain.
Output: Enhanced root layout with metadataBase, static OG image, robots.ts, sitemap.ts, and next.config with build-time validation.
</objective>

<execution_context>
@/Users/pierrebeunardeau/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierrebeunardeau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-production-deployment/13-RESEARCH.md

# Existing files to modify
@website/src/app/layout.tsx
@website/next.config.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update root layout with metadataBase and enhanced metadata</name>
  <files>website/src/app/layout.tsx, website/next.config.ts</files>
  <action>
Update website/src/app/layout.tsx:
1. Add metadataBase: new URL('https://galileoprotocol.io')
2. Update title to use template pattern: { default: 'Galileo Protocol', template: '%s | Galileo Protocol' }
3. Update description to: "Open standard for luxury product authentication and provenance"
4. Add openGraph metadata:
   - title, description, url, siteName: 'Galileo Protocol'
   - locale: 'en_US', type: 'website'
5. Add twitter metadata:
   - card: 'summary_large_image'
   - title, description matching openGraph
6. Add alternates.canonical: '/'

Update website/next.config.ts:
1. Add typescript.ignoreBuildErrors: false (enforce TypeScript errors)
2. Add eslint.ignoreDuringBuilds: false (enforce ESLint during builds)

Keep existing font configuration unchanged. Use the Metadata type from next.
  </action>
  <verify>
Run `cd /Users/pierrebeunardeau/GalileoLuxury/website && npm run build` - should complete without errors.
Inspect output for meta tags in .next/server/app/page.html or use build logs.
  </verify>
  <done>
Root layout exports metadata with metadataBase pointing to https://galileoprotocol.io.
next.config.ts enforces TypeScript and ESLint during builds.
  </done>
</task>

<task type="auto">
  <name>Task 2: Create OG image, robots.ts, and sitemap.ts</name>
  <files>website/src/app/opengraph-image.tsx, website/src/app/robots.ts, website/src/app/sitemap.ts</files>
  <action>
Create website/src/app/opengraph-image.tsx:
1. Import ImageResponse from 'next/og'
2. Export const alt = 'Galileo Protocol - Open standard for luxury product authentication'
3. Export const size = { width: 1200, height: 630 }
4. Export const contentType = 'image/png'
5. Export default async function Image() returning ImageResponse with:
   - Obsidian background: linear-gradient(135deg, #0a0a0a 0%, #1a1a1a 50%, #0a0a0a 100%)
   - Gold accent color: #D4AF37 for logo/text highlights
   - "Galileo Protocol" title (80px, white, bold)
   - "Open standard for luxury authentication" subtitle (32px, #A3A3A3)
   - Subtle border or accent line in gold
   - Use CSS-in-JS style objects (ImageResponse doesn't support Tailwind)

Create website/src/app/robots.ts:
1. Import MetadataRoute from 'next'
2. Export default function robots(): MetadataRoute.Robots
3. Return { rules: { userAgent: '*', allow: '/' }, sitemap: 'https://galileoprotocol.io/sitemap.xml' }

Create website/src/app/sitemap.ts:
1. Import MetadataRoute from 'next'
2. Export default function sitemap(): MetadataRoute.Sitemap
3. Return array of URLs:
   - / (home, priority 1, changeFrequency: 'monthly')
   - /docs (priority 0.8)
   - /docs/getting-started (priority 0.8)
   - /specifications (priority 0.8)
   - /blog (priority 0.7, changeFrequency: 'weekly')
4. Use lastModified: new Date() for all entries
  </action>
  <verify>
Run `cd /Users/pierrebeunardeau/GalileoLuxury/website && npm run build`.
Check that build output includes opengraph-image generation.
Verify robots.ts and sitemap.ts compile without errors.
  </verify>
  <done>
OG image file exists at website/src/app/opengraph-image.tsx with obsidian design.
robots.ts exports crawling rules allowing all.
sitemap.ts exports sitemap with main pages.
  </done>
</task>

</tasks>

<verification>
1. `npm run build` completes successfully in website/ directory
2. Build output mentions opengraph-image generation
3. No TypeScript or ESLint errors during build
4. All 5 files exist and export correct types
</verification>

<success_criteria>
- metadataBase set to https://galileoprotocol.io in root layout
- OG image uses obsidian design with gold accents
- robots.ts allows all crawlers and references sitemap
- sitemap.ts includes main pages (/, /docs, /specifications, /blog)
- next.config.ts enforces ignoreBuildErrors: false and ignoreDuringBuilds: false
- `npm run build` passes with no errors
</success_criteria>

<output>
After completion, create `.planning/phases/13-production-deployment/13-01-SUMMARY.md`
</output>
