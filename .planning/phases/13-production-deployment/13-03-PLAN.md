---
phase: 13-production-deployment
plan: 03
type: execute
wave: 2
depends_on: ["13-01"]
files_modified:
  - specifications/**/*.md
  - specifications/**/*.json
  - specifications/**/*.jsonld
  - governance/**/*.md
  - website/src/components/**/*.tsx
autonomous: true

must_haves:
  truths:
    - "No references to galileo.luxury remain in codebase"
    - "All canonical URLs point to galileoprotocol.io"
    - "Above-fold images have priority prop"
    - "Local Lighthouse scores >90 on all categories"
  artifacts:
    - path: "specifications/"
      provides: "Specifications with updated domain"
      contains: "galileoprotocol.io"
    - path: "website/src/components/sections/Hero.tsx"
      provides: "Hero with optimized images"
      contains: "priority"
  key_links:
    - from: "specifications/**"
      to: "galileoprotocol.io"
      via: "domain references"
      pattern: "galileoprotocol\\.io"
---

<objective>
Replace all galileo.luxury references with galileoprotocol.io and optimize images for Lighthouse performance.

Purpose: Ensure consistent domain across all content and achieve >90 Lighthouse scores locally before deployment.
Output: Updated specifications and components with correct domain, optimized hero images with priority prop.
</objective>

<execution_context>
@/Users/pierrebeunardeau/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierrebeunardeau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/13-production-deployment/13-RESEARCH.md
@.planning/phases/13-production-deployment/13-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace galileo.luxury with galileoprotocol.io across codebase</name>
  <files>specifications/**/*.md, specifications/**/*.json, specifications/**/*.jsonld, governance/**/*.md, website/src/components/**/*.tsx</files>
  <action>
Use sed or similar to replace all occurrences of galileo.luxury with galileoprotocol.io.

Target directories:
1. specifications/ - All .md, .json, .jsonld files (core specification files)
2. governance/ - All .md files (governance docs)
3. website/src/components/ - Any .tsx files with domain references

Command pattern:
```bash
# For macOS sed (BSD):
find /path/to/dir -type f \( -name "*.md" -o -name "*.json" -o -name "*.jsonld" -o -name "*.tsx" \) -exec sed -i '' 's/galileo\.luxury/galileoprotocol.io/g' {} +
```

DO NOT modify:
- .planning/ directory (historical records)
- .git/ directory
- node_modules/

After replacement:
1. Run grep to verify no galileo.luxury references remain in specifications/, governance/, website/src/
2. Run `npm run build` in website/ to ensure no TypeScript errors from changes
  </action>
  <verify>
Run: `grep -r "galileo\.luxury" specifications/ governance/ website/src/ 2>/dev/null | wc -l` should return 0.
Run: `cd website && npm run build` should complete without errors.
  </verify>
  <done>
Zero references to galileo.luxury in specifications/, governance/, and website/src/ directories.
Build passes with updated domain references.
  </done>
</task>

<task type="auto">
  <name>Task 2: Audit and optimize images for Lighthouse performance</name>
  <files>website/src/components/sections/Hero.tsx, website/src/components/sections/*.tsx</files>
  <action>
Audit all Image components in website/src/components/:

1. Hero.tsx - Check for hero/above-fold images:
   - Add `priority` prop to main hero image (if any)
   - Ensure proper `sizes` prop for responsive images

2. Check other sections for above-fold images:
   - Architecture.tsx (if has images)
   - Features.tsx (if has images)
   - Only first visible image should have priority

3. Run local Lighthouse audit:
   ```bash
   cd website
   npm run build
   npm run start &
   # In another terminal or after server starts:
   npx lighthouse http://localhost:3000 --output=json --output-path=./lighthouse-report.json --chrome-flags="--headless"
   ```

4. Check scores:
   - Extract scores from lighthouse-report.json
   - All categories should be >90

If scores are below 90, common fixes:
- Add `sizes` prop to all Image components
- Ensure no images use loading="lazy" in hero section
- Check for blocking resources
  </action>
  <verify>
Run local Lighthouse: `npx lighthouse http://localhost:3000 --output=html --output-path=./lighthouse.html --chrome-flags="--headless"`.
Open lighthouse.html and verify all scores >90.
Alternatively, use JSON output and check categories.performance, .accessibility, .best-practices, .seo are all >= 0.9.
  </verify>
  <done>
Above-fold images have priority prop where needed.
Local Lighthouse audit shows >90 on Performance, Accessibility, Best Practices, and SEO.
  </done>
</task>

</tasks>

<verification>
1. `grep -r "galileo\.luxury" specifications/ governance/ website/src/` returns no matches
2. `npm run build` in website/ completes successfully
3. Local Lighthouse audit shows all categories >90
4. Hero images have proper priority and sizes props
</verification>

<success_criteria>
- All galileo.luxury references replaced with galileoprotocol.io
- No TypeScript/build errors after replacement
- Above-fold images optimized with priority prop
- Local Lighthouse scores: Performance >90, Accessibility >90, Best Practices >90, SEO >90
</success_criteria>

<output>
After completion, create `.planning/phases/13-production-deployment/13-03-SUMMARY.md`
</output>
