---
phase: 11-specifications-viewer
plan: 03
type: execute
wave: 3
depends_on: ["11-02"]
files_modified: []
autonomous: false

must_haves:
  truths:
    - "User can browse all 42 specifications without errors"
    - "User sees correct status badges for each specification"
    - "User can view specification details with proper formatting"
    - "User can access JSON schema files with readable display"
    - "Navigation is consistent with /docs portal UX"
  artifacts: []
  key_links: []
---

<objective>
Verify that the specifications viewer is fully functional, all 42 specs render correctly, status badges display accurate information, and the user experience is consistent with the established /docs portal.

Purpose: Ensure quality through human verification before considering Phase 11 complete. Catch any rendering issues, broken links, or missing specifications.

Output: Approved specifications viewer ready for production deployment.
</objective>

<execution_context>
@/Users/pierrebeunardeau/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierrebeunardeau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/11-specifications-viewer/11-01-SUMMARY.md
@.planning/phases/11-specifications-viewer/11-02-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete specifications viewer with:
- Sidebar navigation for 9 categories
- Landing page with category grid
- 9 category listing pages
- 42 individual specification pages (markdown + JSON schemas)
- Status badge system (Standard/Active/Draft)
- Metadata display (version, date, spec ID)
- MDX rendering with syntax highlighting
- JSON schema formatted viewer
  </what-built>

  <how-to-verify>
**1. Build verification**
```bash
cd website
npm run build
```
- Should complete without errors
- Check build output shows all spec routes generated

**2. Navigation verification**

Visit http://localhost:3000/specifications (or production URL)

- [ ] Landing page displays 9 category cards with correct spec counts
- [ ] Sidebar shows all 9 categories: Architecture, Compliance, Contracts, Crypto, Identity, Infrastructure, Resolver, Schemas, Token
- [ ] Clicking category in sidebar navigates to category page
- [ ] Sidebar sections are collapsible (ChevronDown icon rotates)
- [ ] Active page highlighted in gold in sidebar

**3. Category page verification**

Test each category (spot check 3-4):
- http://localhost:3000/specifications/identity
- http://localhost:3000/specifications/schemas
- http://localhost:3000/specifications/token

For each category page:
- [ ] Category name displayed as H1 (capitalized)
- [ ] Spec count shown ("X specifications in [Category] category")
- [ ] All specs in category listed as cards
- [ ] Each card shows: title, status badge, version, last updated date
- [ ] Status badges use correct colors (Standard=blue, Active=green, Draft=yellow)
- [ ] Grid layout responsive (2 cols on desktop, 1 on mobile)
- [ ] angle-glow hover effect works on cards
- [ ] Clicking card navigates to spec detail page

**4. Specification detail page verification**

Test markdown specs:
- http://localhost:3000/specifications/identity/DID-DOCUMENT
- http://localhost:3000/specifications/crypto/CRYPTO-AGILITY
- http://localhost:3000/specifications/token/ownership-transfer

For each markdown spec:
- [ ] Title displays correctly (from first # heading)
- [ ] Metadata bar shows: status badge, version, last updated, spec ID
- [ ] Markdown content renders with proper formatting
- [ ] Code blocks have syntax highlighting (shiki)
- [ ] Headings, lists, tables, links all render correctly
- [ ] Prose styling applied (readable typography, proper spacing)
- [ ] Page is readable and professional

Test JSON schema specs:
- http://localhost:3000/specifications/schemas/identity/galileo-vc.schema
- http://localhost:3000/specifications/schemas/extensions/terroir-provenance.schema

For each JSON schema:
- [ ] Title extracted from schema
- [ ] Metadata shows "Standard" status (default for JSON)
- [ ] JSON formatted and readable (indented, not minified)
- [ ] Syntax highlighting or at least monospace font
- [ ] Scrollable if JSON is long

**5. Complete coverage verification**

```bash
# Check all 42 specs are accessible
cd website
npm run build
# Count generated routes
grep -c "/specifications/" .next/BUILD_ID || echo "Check build output manually"
```

Spot check specs from each category:
- [ ] Architecture: Browse available specs
- [ ] Compliance: Browse available specs
- [ ] Contracts: Browse available specs
- [ ] Crypto: Browse available specs
- [ ] Identity: Browse available specs (should be ~5-7 specs)
- [ ] Infrastructure: Browse available specs
- [ ] Resolver: Browse available specs
- [ ] Schemas: Browse available specs (should be ~5-7 JSON files)
- [ ] Token: Browse available specs

**6. Requirements verification (SPEC-01 to SPEC-04)**

- [ ] SPEC-01: User can browse specifications by category ✓
- [ ] SPEC-02: User sees status badges (Standard, Active, Draft) ✓
- [ ] SPEC-03: User can view specification details with metadata ✓
- [ ] SPEC-04: User can access JSON schema files directly ✓

**7. UX consistency verification**

Compare with /docs portal:
- [ ] Sidebar navigation style matches /docs
- [ ] Layout structure matches /docs (sidebar left, content right)
- [ ] Typography consistent (same prose styling)
- [ ] Color scheme consistent (platinum, antique gold, obsidian)
- [ ] Responsive behavior matches /docs (sidebar hidden on mobile)

**8. Error checking**

- [ ] No 404 errors when navigating between pages
- [ ] No console errors in browser dev tools
- [ ] No broken links to spec files
- [ ] All status badges display (no missing/undefined)
- [ ] All dates and versions display (no "undefined" or "null")

**9. Mobile verification (optional but recommended)**

Resize browser to mobile width or test on phone:
- [ ] Sidebar hidden on mobile (hamburger menu would be Phase 12)
- [ ] Content readable on mobile
- [ ] Cards stack vertically (1 column)
- [ ] No horizontal scroll

**10. Performance verification**

- [ ] Pages load quickly (static generation, should be fast)
- [ ] No client-side JavaScript errors
- [ ] Syntax highlighting renders (not blank code blocks)
  </how-to-verify>

  <resume-signal>
After completing verification checklist, respond with:
- "approved" if all checks pass
- Describe specific issues if any items fail (e.g., "Identity category shows 0 specs, broken link to DID-DOCUMENT")

If issues found, Claude will fix them before continuing.
  </resume-signal>
</task>

</tasks>

<verification>
Human approval required before Phase 11 can be marked complete.
</verification>

<success_criteria>
Verification complete when:
- [ ] Human has tested all verification steps
- [ ] All SPEC-01 through SPEC-04 requirements verified working
- [ ] No critical bugs or broken links found
- [ ] UX is consistent with /docs portal
- [ ] All 42 specifications accessible and render correctly
- [ ] Human approves with "approved" signal
</success_criteria>

<output>
After approval, create `.planning/phases/11-specifications-viewer/11-03-SUMMARY.md`
</output>
