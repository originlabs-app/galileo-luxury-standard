---
phase: 09-landing-page
plan: 04
type: execute
wave: 3
depends_on: ["09-02", "09-03"]
files_modified:
  - website/src/components/layout/Footer.tsx
  - website/src/app/page.tsx
autonomous: false

must_haves:
  truths:
    - "User sees footer with GitHub, docs, governance, and legal links"
    - "User sees complete landing page with all sections in correct order"
    - "All sections render correctly on desktop and mobile"
  artifacts:
    - path: "website/src/components/layout/Footer.tsx"
      provides: "Site footer with navigation links"
      exports: ["Footer"]
    - path: "website/src/app/page.tsx"
      provides: "Complete landing page composition"
      exports: ["default"]
  key_links:
    - from: "website/src/app/page.tsx"
      to: "website/src/components/sections/*"
      via: "imports and rendering"
      pattern: "import.*from.*components/sections"
    - from: "website/src/app/page.tsx"
      to: "website/src/components/layout/*"
      via: "imports and rendering"
      pattern: "import.*from.*components/layout"
---

<objective>
Create Footer component and wire all sections together in page.tsx for a complete landing page.

Purpose: Complete the landing page with all required sections, enabling visual verification.
Output: Fully functional landing page with all 7 sections.
</objective>

<execution_context>
@/Users/pierrebeunardeau/.claude/get-shit-done/workflows/execute-plan.md
@/Users/pierrebeunardeau/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/09-landing-page/09-RESEARCH.md
@website/src/app/globals.css
@website/src/components/layout/Navigation.tsx
@website/src/components/sections/Hero.tsx
@website/src/components/sections/ValueProposition.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Footer Component</name>
  <files>website/src/components/layout/Footer.tsx</files>
  <action>
Create Footer.tsx with navigation links organized in columns.

Required link groups (from ROADMAP requirements NAV-03):
1. **Resources**: Documentation, Specifications, GitHub
2. **Governance**: Governance, TSC Charter, Contributing
3. **Legal**: License (Apache 2.0), Privacy Policy, Terms

Structure:
```typescript
import Link from 'next/link'
import { Github } from 'lucide-react'

const footerLinks = {
  resources: [
    { label: 'Documentation', href: '/docs' },
    { label: 'Specifications', href: '/specs' },
    { label: 'GitHub', href: 'https://github.com/originlabs-app/galileo-luxury-standard', external: true },
  ],
  governance: [
    { label: 'Governance', href: '/governance' },
    { label: 'TSC Charter', href: '/governance/tsc' },
    { label: 'Contributing', href: '/docs/contributing' },
  ],
  legal: [
    { label: 'License (Apache 2.0)', href: '/license' },
    { label: 'Privacy Policy', href: '/privacy' },
    { label: 'Terms', href: '/terms' },
  ],
}

export function Footer() {
  return (
    <footer className="border-t border-[rgba(229,229,229,0.08)] bg-[#050505]">
      <div className="container py-16">
        <div className="grid md:grid-cols-4 gap-12">
          {/* Logo Column */}
          <div className="md:col-span-1">
            <Link href="/" className="flex items-center gap-3 mb-4">
              <div className="w-8 h-8 rounded-lg bg-gradient-to-br from-[#D4AF37] to-[#B8973A] flex items-center justify-center">
                <span className="text-[#050505] font-serif font-bold text-lg">G</span>
              </div>
              <span className="font-serif text-xl font-medium text-[#E5E5E5]">
                GALILEO
              </span>
            </Link>
            <p className="text-[#A3A3A3] text-sm mb-4">
              The open standard for luxury product authenticity and compliance.
            </p>
            <a
              href="https://github.com/originlabs-app/galileo-luxury-standard"
              target="_blank"
              rel="noopener noreferrer"
              className="inline-flex items-center gap-2 text-[#A3A3A3] hover:text-[#E5E5E5] transition-colors"
            >
              <Github className="w-5 h-5" />
              <span className="text-sm">Star on GitHub</span>
            </a>
          </div>

          {/* Resources Column */}
          <div>
            <h4 className="text-[#E5E5E5] font-sans font-medium mb-4">Resources</h4>
            <ul className="space-y-3">
              {footerLinks.resources.map((link) => (
                <li key={link.href}>
                  {link.external ? (
                    <a
                      href={link.href}
                      target="_blank"
                      rel="noopener noreferrer"
                      className="text-[#A3A3A3] hover:text-[#E5E5E5] text-sm transition-colors"
                    >
                      {link.label}
                    </a>
                  ) : (
                    <Link
                      href={link.href}
                      className="text-[#A3A3A3] hover:text-[#E5E5E5] text-sm transition-colors"
                    >
                      {link.label}
                    </Link>
                  )}
                </li>
              ))}
            </ul>
          </div>

          {/* Governance Column */}
          <div>
            <h4 className="text-[#E5E5E5] font-sans font-medium mb-4">Governance</h4>
            <ul className="space-y-3">
              {footerLinks.governance.map((link) => (
                <li key={link.href}>
                  <Link
                    href={link.href}
                    className="text-[#A3A3A3] hover:text-[#E5E5E5] text-sm transition-colors"
                  >
                    {link.label}
                  </Link>
                </li>
              ))}
            </ul>
          </div>

          {/* Legal Column */}
          <div>
            <h4 className="text-[#E5E5E5] font-sans font-medium mb-4">Legal</h4>
            <ul className="space-y-3">
              {footerLinks.legal.map((link) => (
                <li key={link.href}>
                  <Link
                    href={link.href}
                    className="text-[#A3A3A3] hover:text-[#E5E5E5] text-sm transition-colors"
                  >
                    {link.label}
                  </Link>
                </li>
              ))}
            </ul>
          </div>
        </div>

        {/* Copyright */}
        <div className="border-t border-[rgba(229,229,229,0.08)] mt-12 pt-8 text-center">
          <p className="text-[#A3A3A3] text-sm">
            © {new Date().getFullYear()} Galileo Luxury Standard. Licensed under Apache 2.0.
          </p>
        </div>
      </div>
    </footer>
  )
}
```

Use same logo styling as Navigation.tsx for consistency.
GitHub icon from Lucide for the "Star on GitHub" link.
  </action>
  <verify>
File exists at `website/src/components/layout/Footer.tsx`.
Contains `export function Footer()`.
Contains 3 link columns (resources, governance, legal).
Contains GitHub link to correct repository.
Contains Apache 2.0 mention in copyright.
  </verify>
  <done>
Footer.tsx exports Footer component with 4-column layout (logo + 3 link groups) and copyright.
  </done>
</task>

<task type="auto">
  <name>Task 2: Compose Complete Landing Page</name>
  <files>website/src/app/page.tsx</files>
  <action>
Replace the default Next.js page with a composition of all landing page sections.

Section order (top to bottom):
1. Hero (existing)
2. ValueProposition (existing)
3. Architecture (new - from 09-02)
4. Features (new - from 09-02)
5. Standards (new - from 09-03)
6. Regulatory (new - from 09-03)
7. Footer (new - from this plan)

Note: Navigation is rendered in layout.tsx, not page.tsx.

```typescript
import { Hero } from '@/components/sections/Hero'
import { ValueProposition } from '@/components/sections/ValueProposition'
import { Architecture } from '@/components/sections/Architecture'
import { Features } from '@/components/sections/Features'
import { Standards } from '@/components/sections/Standards'
import { Regulatory } from '@/components/sections/Regulatory'
import { Footer } from '@/components/layout/Footer'

export default async function Home() {
  return (
    <main>
      <Hero />
      <ValueProposition />
      <Architecture />
      <Features />
      <Standards />
      <Regulatory />
      <Footer />
    </main>
  )
}
```

IMPORTANT:
- Use `async function` because Features is async (uses CodeBlock with Shiki)
- Import paths use `@/` alias (configured in tsconfig.json)
- No 'use client' - entire page is Server Component for optimal performance
  </action>
  <verify>
File at `website/src/app/page.tsx` contains all 7 component imports.
Contains `export default async function Home()`.
Renders components in correct order: Hero, ValueProposition, Architecture, Features, Standards, Regulatory, Footer.
No 'use client' directive present.
  </verify>
  <done>
page.tsx composes all 7 sections in correct order as an async Server Component.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Complete landing page with all required sections:
- Hero with tagline and CTAs (LAND-01)
- Value proposition cards - Heritage, Precision, Compliance (LAND-02)
- Architecture diagram showing hybrid model (LAND-03)
- Standards compliance table (LAND-04)
- Regulatory readiness with deadlines (LAND-05)
- Code examples with JSON-LD and Solidity (part of Features)
- Navigation header with mobile menu (NAV-01, NAV-02)
- Footer with links (NAV-03)
  </what-built>
  <how-to-verify>
1. Start development server: `cd website && npm run dev`
2. Open http://localhost:3000 in browser
3. Verify each section renders:
   - [ ] Hero: Gold gradient title, version badge, two CTAs visible
   - [ ] Value Proposition: Three cards (Heritage, Precision, Compliance) with icons
   - [ ] Architecture: SVG diagram with three layers (off-chain, resolver, on-chain)
   - [ ] Features: Two code blocks with syntax highlighting (JSON-LD, Solidity)
   - [ ] Standards: Six standards with icons and badges
   - [ ] Regulatory: Three regulations with deadlines and color coding
   - [ ] Footer: Four columns with links and GitHub icon
4. Test mobile view (resize to < 768px):
   - [ ] Hamburger menu appears
   - [ ] Sections stack vertically
   - [ ] Text remains readable
5. Check console for errors: Should be clean (no React/Next.js errors)
  </how-to-verify>
  <resume-signal>
Type "approved" if all sections render correctly, or describe specific issues.
  </resume-signal>
</task>

</tasks>

<verification>
1. `cat website/src/components/layout/Footer.tsx` - contains Footer with 4 columns
2. `cat website/src/app/page.tsx` - imports and renders all 7 sections
3. `cd website && npm run dev` - starts without errors
4. Visual verification of complete page at http://localhost:3000
</verification>

<success_criteria>
- Footer.tsx displays logo, 3 link columns, and copyright
- page.tsx composes all sections in correct order
- Development server starts without errors
- All 9 requirements addressed: LAND-01 through LAND-06, NAV-01 through NAV-03
- Page renders correctly on desktop and mobile viewports
</success_criteria>

<output>
After completion, create `.planning/phases/09-landing-page/09-04-SUMMARY.md`
</output>
