---
phase: 09.1-spatial-ui
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - website/src/app/globals.css
  - website/src/app/layout.tsx
autonomous: true

must_haves:
  truths:
    - "Typography uses Satoshi for body, Outfit for titles"
    - "Spatial background has mesh gradient depth"
    - "Font variables are defined and accessible"
  artifacts:
    - path: "website/src/app/globals.css"
      provides: "Spatial design system foundation"
      exports: ["--font-satoshi", "--font-outfit", "--deep-plum", "--midnight-blue"]
    - path: "website/src/app/layout.tsx"
      provides: "Font imports via next/font"
      exports: ["satoshi", "outfit"]
---

<objective>
Establish the typography "Swiss-Luxe" stack and spatial background mesh gradients as foundation for all subsequent UI updates.

Purpose: Foundation layer that all other Phase 9.1 components build upon.
Output: Updated globals.css with new fonts and spatial background.
</objective>

<context>
@.planning/phases/09.1-spatial-ui/09.1-RESEARCH.md
@website/src/app/globals.css
@website/src/app/layout.tsx
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add Font Imports to layout.tsx</name>
  <files>website/src/app/layout.tsx</files>
  <action>
Import Satoshi and Outfit fonts using next/font.

Satoshi is available from Fontshare but we'll use a local/Google alternative approach.
For simplicity, use Inter as fallback and Outfit from Google Fonts.

**Update:** Use Google Fonts for Outfit, keep system sans as Satoshi fallback until we can add the actual Satoshi font files.

```typescript
import { Outfit, Inter, Cormorant_Garamond } from 'next/font/google'

const outfit = Outfit({
  subsets: ['latin'],
  variable: '--font-outfit',
  display: 'swap',
})

const inter = Inter({
  subsets: ['latin'],
  variable: '--font-satoshi', // Will be replaced with Satoshi later
  display: 'swap',
})

const cormorant = Cormorant_Garamond({
  subsets: ['latin'],
  weight: ['400', '500', '600'],
  style: ['normal', 'italic'],
  variable: '--font-serif',
  display: 'swap',
})

// In the body className:
<body className={`${outfit.variable} ${inter.variable} ${cormorant.variable} ...`}>
```
  </action>
  <verify>
layout.tsx imports Outfit, Inter, and Cormorant_Garamond from next/font/google.
CSS variables --font-outfit, --font-satoshi, --font-serif are applied to body.
  </verify>
</task>

<task type="auto">
  <name>Task 2: Update Typography System in globals.css</name>
  <files>website/src/app/globals.css</files>
  <action>
Update the @theme block to use new font variables and add utility classes.

1. Update font-family definitions:
```css
@theme {
  /* Typography - Swiss-Luxe Stack */
  --font-satoshi: var(--font-satoshi), ui-sans-serif, system-ui, sans-serif;
  --font-outfit: var(--font-outfit), ui-sans-serif, system-ui, sans-serif;
  --font-serif: var(--font-serif), 'Cormorant Garamond', Georgia, serif;
  --font-mono: 'JetBrains Mono', ui-monospace, monospace;
}
```

2. Update base styles:
```css
body {
  font-family: var(--font-satoshi);
}

h1, h2, h3 {
  font-family: var(--font-outfit);
  font-weight: 600;
}

.italic-accent {
  font-family: var(--font-serif);
  font-style: italic;
}
```
  </action>
  <verify>
globals.css defines --font-satoshi, --font-outfit, --font-serif.
Body uses --font-satoshi.
h1, h2, h3 use --font-outfit.
  </verify>
</task>

<task type="auto">
  <name>Task 3: Add Spatial Background and Color Palette</name>
  <files>website/src/app/globals.css</files>
  <action>
Add new "Deep Space" colors and spatial background utility.

1. Add to @theme block:
```css
@theme {
  /* Colors - Deep Space Palette */
  --obsidian: #050505;
  --surface: #0a0a0a;
  --deep-plum: #1A0B2E;
  --midnight-blue: #020617;
  --gold: #D4AF37;
  --gold-dim: #B8973A;
  --platinum: #E5E5E5;
  --dim: #A3A3A3;
  --precision-blue: #00A3FF;
}
```

2. Add spatial background utility class:
```css
.spatial-background {
  background-color: var(--obsidian);
  background-image:
    radial-gradient(ellipse at 0% 0%, rgba(26, 11, 46, 0.15) 0px, transparent 50%),
    radial-gradient(ellipse at 100% 100%, rgba(0, 163, 255, 0.05) 0px, transparent 50%),
    radial-gradient(ellipse at 50% 50%, rgba(26, 11, 46, 0.08) 0px, transparent 70%);
}
```

3. Apply to main layout or body:
```css
body {
  background-color: var(--obsidian);
  color: var(--platinum);
}

main {
  @apply spatial-background;
}
```

Or add `.spatial-background` to the main element in layout.tsx.
  </action>
  <verify>
globals.css defines --deep-plum and --midnight-blue.
.spatial-background class exists with multi-layer radial gradients.
Body background is --obsidian.
  </verify>
</task>

<task type="auto">
  <name>Task 4: Add Angle Glow Utility Classes</name>
  <files>website/src/app/globals.css</files>
  <action>
Add the angle-glow border effect as a reusable utility class.

```css
/* Angle Glow Border Effect */
.angle-glow {
  position: relative;
  border-radius: 12px;
  background: var(--surface);
}

.angle-glow::before {
  content: '';
  position: absolute;
  inset: -1px;
  border-radius: inherit;
  padding: 1px;
  background: conic-gradient(
    from 45deg,
    transparent 0deg,
    var(--gold) 30deg,
    transparent 60deg,
    transparent 180deg,
    var(--gold) 210deg,
    transparent 240deg
  );
  -webkit-mask:
    linear-gradient(#fff 0 0) content-box,
    linear-gradient(#fff 0 0);
  -webkit-mask-composite: xor;
  mask-composite: exclude;
  opacity: 0;
  transition: opacity 0.3s ease;
  pointer-events: none;
}

.angle-glow:hover::before {
  opacity: 1;
}

/* Variant: Always visible (subtle) */
.angle-glow-ambient::before {
  opacity: 0.3;
}

.angle-glow-ambient:hover::before {
  opacity: 1;
}
```
  </action>
  <verify>
.angle-glow class exists with conic-gradient pseudo-element.
:hover state shows the glow effect.
.angle-glow-ambient variant exists for always-visible subtle glow.
  </verify>
</task>

<task type="auto">
  <name>Task 5: Add Micro-Haptic Button Utility</name>
  <files>website/src/app/globals.css</files>
  <action>
Add the micro-haptic feedback utility for buttons.

```css
/* Micro-Haptic Feedback */
.button-haptic {
  transition: transform 0.1s ease-out;
}

.button-haptic:active {
  transform: scale(0.98);
}

/* Respect reduced motion */
@media (prefers-reduced-motion: reduce) {
  .button-haptic:active {
    transform: none;
  }
}
```
  </action>
  <verify>
.button-haptic class exists with :active scale(0.98).
Reduced motion media query disables the transform.
  </verify>
</task>

</tasks>

<verification>
1. `grep -n "font-outfit" website/src/app/layout.tsx` — font import exists
2. `grep -n "spatial-background" website/src/app/globals.css` — utility class exists
3. `grep -n "angle-glow" website/src/app/globals.css` — angle glow effect exists
4. `grep -n "deep-plum" website/src/app/globals.css` — new color defined
5. `cd website && npm run dev` — no build errors
</verification>

<success_criteria>
- Typography system uses Outfit for titles, Inter (Satoshi proxy) for body
- Spatial background mesh gradients add depth to the page
- Angle glow utility ready for component use
- Micro-haptic button utility ready for CTAs
- All utilities respect prefers-reduced-motion
</success_criteria>
