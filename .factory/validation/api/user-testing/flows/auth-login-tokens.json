{
  "groupId": "auth-login-tokens",
  "testedAt": "2026-03-03T14:15:35.000Z",
  "credentials": {
    "account": "flow2-user@galileo.test",
    "namespace": "flow2"
  },
  "toolsUsed": ["curl"],
  "assertions": [
    {
      "id": "VAL-API-004",
      "title": "Successful login",
      "status": "pass",
      "steps": [
        { "action": "POST /auth/register with user info", "expected": "Account created (201)", "observed": "Account created (201)" },
        { "action": "POST /auth/login with valid credentials", "expected": "Returns 200 with tokens", "observed": "Returned 200 with accessToken and refreshToken" },
        { "action": "Decode JWT", "expected": "exp - iat is ~900 (15 min)", "observed": "exp - iat is 900 (1772548186 - 1772547286 = 900)" }
      ],
      "evidence": {
        "network": "POST /auth/login -> 200"
      },
      "issues": null
    },
    {
      "id": "VAL-API-005",
      "title": "Login wrong password",
      "status": "pass",
      "steps": [
        { "action": "POST /auth/login with valid email and wrong password", "expected": "Returns 401 with generic error message", "observed": "Returned 401 Unauthorized with 'Invalid email or password' message" }
      ],
      "evidence": {
        "network": "POST /auth/login -> 401"
      },
      "issues": null
    },
    {
      "id": "VAL-API-006",
      "title": "Login non-existent email",
      "status": "pass",
      "steps": [
        { "action": "POST /auth/login with non-existent email", "expected": "Returns 401 with generic error message", "observed": "Returned 401 Unauthorized with 'Invalid email or password' message (same as wrong password)" }
      ],
      "evidence": {
        "network": "POST /auth/login -> 401"
      },
      "issues": null
    },
    {
      "id": "VAL-API-007",
      "title": "Token refresh",
      "status": "pass",
      "steps": [
        { "action": "POST /auth/refresh with valid refresh token", "expected": "Returns 200 with new tokens", "observed": "Returned 200 with new accessToken and refreshToken" }
      ],
      "evidence": {
        "network": "POST /auth/refresh -> 200"
      },
      "issues": null
    },
    {
      "id": "VAL-API-017",
      "title": "Invalid refresh token rejected",
      "status": "pass",
      "steps": [
        { "action": "POST /auth/refresh with garbage token", "expected": "Returns 401", "observed": "Returned 401 Unauthorized with 'Invalid or expired refresh token' message" }
      ],
      "evidence": {
        "network": "POST /auth/refresh -> 401"
      },
      "issues": null
    }
  ],
  "frictions": [],
  "blockers": [],
  "summary": "Tested 5 assertions (VAL-API-004, VAL-API-005, VAL-API-006, VAL-API-007, VAL-API-017). All passed as expected."
}
